<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
        xmlns:android="http://schemas.android.com/apk/res/android"

        xmlns:card_view="http://schemas.android.com/apk/res-auto"
        card_view:cardBackgroundColor="@color/whiteDark"
        android:id="@+id/card_view"

        android:layout_width="match_parent"
        android:layout_height="wrap_content"

        android:layout_marginTop="@dimen/cardViewPadding"
        android:layout_marginStart="@dimen/leftRightCardMargin"
        android:layout_marginEnd="@dimen/leftRightCardMargin"
        card_view:cardCornerRadius="4dp">

    <LinearLayout
            android:id="@+id/linear_layout"
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingTop="@dimen/topBottomPadding"
            android:paddingBottom="@dimen/topBottomPadding"
            android:paddingStart="@dimen/leftRightCardPadding"
            android:paddingEnd="@dimen/leftRightCardPadding">
        <TextView
                android:id="@+id/name"
                android:textStyle="bold"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="@dimen/nameSize"/>
        <!-- наверн так деать не надо, но мне прост интересно -->

        <TextView
                android:id="@+id/phone_number"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="@dimen/numberSize"/>
    </LinearLayout>


</androidx.cardview.widget.CardView>